<form method="post" action="{{ path('app_sell_slot_delete', {'id': sell_slot.id}) }}" onsubmit="
return confirm('Voulez-vous vraiment supprimer cette vente ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sell_slot.id) }}">
    <button class="btn-my-danger">Supprimer</button>
</form>
