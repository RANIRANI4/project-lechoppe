<form method="post" action="{{ path('app_sell_slot_delete', {'id': sell_slot.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sell_slot.id) }}">
    <button class="btn">Delete</button>
</form>
