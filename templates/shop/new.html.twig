{% extends 'base.html.twig' %}

{% block title %}New Shop{% endblock %}

{% block body %}

<div class=" w-75 mx-auto">
    <div class="d-flex align-items-center gap-3 py-3">

        <h5>
            Création d'une échoppe
        </h5>
{#        <a href="{{ path('app_user_show', {'id': app.user.id}) }}" class="btn-beige">#}
            Annuler
        </a>
    </div>

    {{ include('shop/_form.html.twig') }}

    <a href="{{ path('app_shop_index') }}">back to list</a>
{% endblock %}
